#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int co1, co2;
	for(co1 = 1; co2 <= 100; co1++)
	{
printf("Enter %d co2: ", co2);
scanf("%d", &co2);
if(co2==100)
break;
	
	}
	
	return 0;
}
